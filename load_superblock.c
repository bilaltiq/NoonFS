#include "types.h"

/*
    Load the superblock (the 0th block in the block device) in preparation
    for load_directory
*/
void load_superblock(address_t RAM_buffer)
{
    // TODO: Load the superblock
}